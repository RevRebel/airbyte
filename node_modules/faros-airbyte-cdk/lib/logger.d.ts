import { Level, Logger } from 'pino';
import { AirbyteLogLevel, AirbyteMessage } from './protocol';
export declare class AirbyteLogger {
    private level;
    constructor(level?: AirbyteLogLevel);
    error(message: string): void;
    warn(message: string): void;
    info(message: string): void;
    debug(message: string): void;
    trace(message: string): void;
    private log;
    write(msg: AirbyteMessage): void;
    /**
     * Creates a Pino Logger writing messages in Airbyte format
     *
     * @param level logging level
     * @returns Pino Logger
     */
    asPino(level?: Level): Logger;
    private static writeMessage;
}
